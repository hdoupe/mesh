from distutils.core import setup

setup(
    name='mesh',
    version='0.1.0',
    packages=['mesh', 'mesh.proxy'],
    license='',
    long_description='Multiple-language Endpoint Submission Handler',
)
